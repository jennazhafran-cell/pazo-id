<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - PAZO.id' : 'PAZO.id - Analisis Trading Harian' %></title>
  <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'Blog analisis trading harian terkini, akurat, dan serba cepat.' %>">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <div class="container nav-bar">
      <a href="/" class="logo">PAZO<span>.id</span></a>
      <form action="/" method="GET" class="search-box">
        <input type="text" name="q" placeholder="Cari analisis..." value="<%= typeof search !== 'undefined' ? search : '' %>">
      </form>
    </div>
  </header>
  <div class="container">
    <%- include('ads-banner') %>
